.row
  .container
    = form_with(model: @tweet, local: true) do |form|
      %h3
        投稿する
      = form.text_field :title, placeholder: "タイトル"
      = form.text_field :image, placeholder: "投稿画像"
      = form.text_area :text, placeholder: "コメント" , rows: "10"
      = form.submit "SEND"
